# =============================================================================
# World Monitor — environment reference
# =============================================================================
# You don't normally edit this file. Run ./deploy.sh and it will prompt for
# these keys on first run and write a .env for you.
#
# This file just documents what each key is, and lets you pre-fill .env by hand
# if you'd rather:  cp .env.example .env  then edit.
# =============================================================================

# --- Cache (auto-generated by deploy.sh; you never type these) ---------------
# Any two random hex strings; they just have to match between runs.
#   openssl rand -hex 32
REDIS_PASSWORD=
REDIS_TOKEN=

# --- AI briefs ---------------------------------------------------------------
# OpenRouter is the route to Claude and other models. https://openrouter.ai
OPENROUTER_API_KEY=

# --- Data sources (all optional; panels degrade gracefully without them) -----
UCDP_ACCESS_TOKEN=      # armed-conflict events — free token, see main README
AISSTREAM_API_KEY=      # live vessel positions — https://aisstream.io
ACLED_EMAIL=            # second conflict-events source — https://acleddata.com
ACLED_PASSWORD=
